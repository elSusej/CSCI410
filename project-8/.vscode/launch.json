{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug VMTranslator (WSL)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/VMTranslator", //change if want to debug different executable
            "args": ["FibonacciElement"], //change if want to translate different file
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "build-VMTranslator" //make sure this matches the name of the task in tasks.json
        }
    ],
        "setupCommands": [ 
    {
        "description": "Enable pretty-printing",
        "text": "-enable-pretty-printing",
        "ignoreFailures": true
    }
]
}